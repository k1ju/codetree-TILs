#include <iostream>
#include <string>
using namespace std;

int main() {

    cout << fixed;
    cout.precision(2);
    double a,b;
    string c;

    cin >> c >> a >> b;
    cout << c << endl << a << endl << b;

    // 여기에 코드를 작성해주세요.
    return 0;
}